
import React from 'react';

// O OneSignal SDK gerencia a recepção de notificações automaticamente.
// Este componente é mantido vazio ou pode ser removido no futuro para limpar a codebase.
export const NotificationListener: React.FC = () => {
  return null;
};
